import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brothersvas.tommyspetshop.app',
  appName: 'UpdatedTommyApp',
  webDir: 'www'
};

export default config;
